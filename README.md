# main

```
git status
git branch namaBrand
git checkout namaBranch
git pull namaBranch (mengambil tugas dari branch lain)
git merge namaBranch (menggabungkan tugas branch main dan branch lain)
git add .
git commit -m "menyelesaikan tugas apa"
git push
```
